{
  "name": "ember-cli-deploy-git-info",
  "version": "0.0.0",
  "description": "Adds all the git details it can find to the deploy context.",
  "directories": {
    "doc": "doc",
    "test": "tests"
  },
  "scripts": {
    "test": "ember try:testall"
  },
  "repository": "",
  "engines": {
    "node": ">= 0.10.0"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "ember-cli": "1.13.1"
  },
  "keywords": [
    "ember-addon",
    "ember-cli-deploy-plugin"
  ],
  "dependencies": {
    "ember-cli-babel": "^5.0.0",
    "ember-cli-deploy-plugin": "^0.1.3",
    "rsvp": "^3.1.0"
  },
  "ember-addon": {
    "configPath": "tests/dummy/config"
  }
}
